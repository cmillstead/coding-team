# Codex Learnings — Pre-flight Checklist

Anti-patterns that Codex cross-model review has repeatedly caught in Claude's plans and diffs.
The `/second-opinion` pre-flight checks every entry here BEFORE dispatching, so Codex spends its
rounds on novel problems instead of the same recurring mistakes. The post-review learning-capture
loop appends new recurring patterns here with the next ID.

**How to use (pre-flight):** the `/second-opinion` engine in `SKILL.md` derives the change's
signals ONCE, then classifies every live entry into exactly TWO buckets — `applicable`
(deep-checked) or `dismissed`. An entry is `dismissed` for EXACTLY ONE of two reasons:
`scope-mismatch` (its `scope:` excludes the current mode — a `scope:diff` entry in a plan review
or a `scope:plan` entry in a diff review; `scope:both` is never scope-dismissed) OR `no-signal`
(an in-scope `provable` category whose signal a grep CONFIRMS absent). EVERYTHING else is
`applicable` and deep-checked: a signal match, a `reasoning-shape` entry, a `floor` entry (P1–P4),
an untagged (newly-appended) entry, or an UNKNOWN/ambiguous derivation. This scopes the expensive deep-check EFFORT to the
applicable subset (typically 4–8 entries, not the full live set) while still ACCOUNTING for
every entry by ID. The tag vocabulary, grep battery, and audit-line format below are read at
SKILL.md Step 1. Bulk-by-category dismissal is BANNED — see the rationalization block.

**Seeded 2026-06-07** from accumulated memory (`feedback_codex_second_opinion`,
`feedback_path-trust-tiers`, `feedback_codex-cross-model-value`). This is v0 — small and honest.
It grows via learning capture, not by inventing slots.

---

## P — Plan patterns

Plans written from memory of the code are optimistic. These are the mismatches Codex catches when
it reads the actual code. Round 1 is mostly structural; round 2 mostly precision (see SKILL.md
round budgeting).

| ID | Pattern | Check before dispatch |
|----|---------|----------------------|
| P1 | `@tags: plan-symbol; reasoning-shape; scope:plan; floor` **Phantom symbol** — plan references a field, type, function, or column that doesn't exist | Grep the codebase for every named symbol the plan touches. If it isn't there, the plan is describing code from memory, not reality. |
| P2 | `@tags: plan-symbol; reasoning-shape; scope:plan; floor` **Wrong API name/shape** — wrong method name, signature, return type, or AST node name | Open the actual definition for every API the plan calls. Codesight/tree-sitter node names and SDK signatures are the usual offenders — confirm, don't recall. |
| P3 | `@tags: plan-symbol; command-grammar; reasoning-shape; scope:plan; floor` **Stale command** — a shell/CLI command whose flags or subcommands no longer exist | Run `--help` (or check the CLI source) for every command the plan tells someone to run. Flags drift; verify the exact invocation. |
| P4 | `@tags: plan-symbol; reasoning-shape; scope:plan; floor` **Wrong identifier** — nonexistent parameter, wrong variable name, or mis-scoped reference | Trace each variable/param named in the plan to its declaration. A name that "sounds right" but isn't there is a round-1 finding. |

| P30 | `@tags: helper-reuse; reasoning-shape; scope:both` **A plan that REUSES/extends an existing shared helper assumes it WORKS as its name/signature implies, but the helper's BODY is a no-op or contract-violating — an `acquire()` that always returns `true`, a `lockName`/`scope` param accepted-but-ignored, a `validate()` that never rejects, a backing table whose `CHECK`/`UNIQUE`/`PK` forbids the new use.** The symbol EXISTS (P1 passes) and the signature FITS (P2 passes), so name+shape checks wave it through — but building on it inherits a silent defect. Caught across 2 rounds on the engram ADR-001 sync-lock plan: the plan said "reuse `SqliteLockProvider`, key on `lockName`, acquire 'sync'", but `SqliteLockProvider.acquire(lockName, ttlMs)` IGNORES `lockName` and hardcodes `INSERT OR IGNORE INTO consolidation_lock (id) VALUES (1)` then returns `true` UNCONDITIONALLY (never `false` on contention — broken as a mutex), with an existing test pinning that broken behavior; AND the target `consolidation_lock` had `id INTEGER PRIMARY KEY CHECK (id = 1)` in a DEPLOYED (immutable) migration, so per-name keying was structurally impossible without a new migration. The "reuse" would have shipped a lock that never locks. | For any plan that REUSES or EXTENDS an existing helper/utility/provider/table, open its BODY (not just its signature): (1) is every parameter actually USED inside the fn? (grep the param name in the body — an accepted-but-ignored arg is the tell); (2) does the return value VARY with outcome? (a mutex `acquire` must be able to return false; a `find` that always returns the first row; a guard that always passes); (3) does the backing DDL PERMIT the new use? — open the `CREATE TABLE`/constraints; a `CHECK`/`UNIQUE`/`PK (id=1)` may forbid it, and if the DDL lives in a DEPLOYED migration it is immutable (needs a NEW migration, never an edit); (4) does an existing TEST pin the current (possibly broken) behavior? — reuse means updating that test. "It already exists with the right signature" is P1/P2-clean but is NOT proof the body honors the contract its name advertises. Prefer a self-contained implementation over reusing a helper whose body you have not read. Pairs with P1 (phantom symbol — there the symbol is ABSENT; here it's PRESENT but lying) and C9 (type-valid-but-degenerate value). |
| P31 | `@tags: metric-aggregate; provable; scope:both` **A metric/aggregate uses a `None`/skip sentinel for "the unit produced nothing," which silently EXCLUDES legitimate zero-score MISSES from the average — inflating it — because the same sentinel also means "not applicable." Distinguish "not applicable" from "zero-score miss" with an EXPLICIT eligibility/capability flag, not a value inferred from the empty output.** When a per-unit scorer returns `Option<Score>` and the aggregate macro-averages only the `Some` values, every `None` vanishes from the denominator. That is correct for units genuinely out-of-scope (a competitor that emits no symbols → N/A), but WRONG for an in-scope unit that simply scored zero (an eligible retriever that retrieved nothing → a real recall miss). Inferring the distinction from the empty output itself (`count==0`, empty list) conflates the two: a competitor and a failed/empty eligible run look identical. Caught on the Axon symbol-precision metric: all-anchor runs, ERRORED runs (error-fallback `symbol: None`), and zero-chunk successful runs were each dropped as `N/A` instead of counted as recall=0, inflating reported recall on exactly the hardest tasks. The fix was an explicit `emits_symbols` capability flag, so an eligible unit's empty output scores as a zero-miss while a non-eligible unit stays N/A. | For any `Option`-returning per-unit metric whose aggregate skips `None`, enumerate WHY a unit can be `None` and split the reasons into "out-of-scope / not-applicable" (correctly excluded) vs "in-scope but scored zero / failed / empty" (must be COUNTED as a zero). If the code infers this from the empty output (`count==0`, empty collection, a null dependency), it conflates them — add an EXPLICIT eligibility flag carried from where capability is known (the producer/trait), and decide zero-vs-N/A on that flag. Check EVERY path that produces the empty output — success-with-nothing, error-fallback, and any "no data" short-circuit — they are siblings of the same inflation. |
| P32 | `@tags: negative-existence; reasoning-shape; scope:plan` **A GROUND/plan asserts a resource or convention does NOT exist (and builds the plan around creating it FRESH / inventing a new location) based on a single empty `find`/`grep` — but it DOES exist elsewhere with an established convention to mirror.** Negative existence claims get waved through with less rigor than positive ones (P1), yet they are just as load-bearing: "there are no fixtures, so we'll make a new dir" silently invents a parallel, inconsistent location. The single search that returned empty missed a sibling directory / different path root (a `-prune` that over-excluded, the wrong subtree). Caught on the Baxter shell-classifier plan: GROUND.md asserted "No `tests/fixtures/` anywhere" (from one pruned `find`), so the plan specified `src-tauri/tests/fixtures/…` + `include_str!(concat!(env!("CARGO_MANIFEST_DIR"), …))`; in fact repo-root `tests/fixtures/fnv-vectors.json` existed, loaded by `lib.rs` via `include_str!("../../tests/fixtures/…")` — the plan should have MIRRORED that proven convention, and `CARGO_MANIFEST_DIR` would have resolved to the wrong root. | For any "X does not exist / there is no established pattern for Y, so we'll create it fresh" claim, verify the ABSENCE with the same rigor as a presence (P1): don't trust one `find`/`grep` (wrong root, over-broad `-prune`, a different dir). Confirm by asking the CONSUMER — "how does a SIBLING test/module already load this kind of resource?" Grep for the capability (`include_str!`, fixture loaders, the file extension) across the WHOLE repo, not one subtree. If a convention exists, the plan MIRRORS it (same location, same load form) instead of inventing a parallel one. A false negative-existence claim makes the plan build duplicate/inconsistent infrastructure — the inverse of P1's phantom-symbol. |
| P33 | `@tags: ambiguity-guard; reasoning-shape; scope:both` **A security/validation guard that resolves AMBIGUITY by enumerating candidate interpretations and DENYING if ANY candidate is dangerous over-denies inputs whose BENIGN interpretation was equally valid.** "A false-positive deny is free because the dangerous reading is malicious" is FALSE whenever a benign reading produces the same token shape — the guard then hard-blocks legitimate commands, eroding trust in the gate (the exact "the gate is dumb, stop trusting it" failure the gate exists to prevent). Caught on the Baxter exec-wrapper plan: a ±1 candidate model for unmodeled wrapper flags emitted two candidate command-positions and denied if either was a deny-family command; `timeout --weird echo sudo` (benign — `echo` prints the word "sudo") denied because `sudo` landed in a candidate slot, even though the real command was `echo`. The premise "the string is malicious" held for `timeout --weird 5 sudo reboot` but not for the token-identical benign case. | For any guard that DISAMBIGUATES by guessing among candidate parses/positions/interpretations, do NOT "deny if any candidate is dangerous" — that over-denies every input whose benign parse shares the shape. Deny ONLY on POSITIVE detection: an UNAMBIGUOUS resolution to a dangerous value. When the parse is ambiguous, fall to the safe NON-deny state — for a 3-way classifier that is ASK (human-gated), never DENY-on-a-guess and never silent ALLOW. Test the dominance property: the new rule must introduce ZERO over-deny relative to the prior behavior while only tightening positively-detected cases. The tell is a rationalization like "a false-positive deny is free / the string is malicious anyway" — verify a BENIGN input can't produce the same candidate shape before accepting it. |

## C — Code / diff patterns

Mistakes Codex catches in actual diffs that same-model audit rounds miss, because Claude's audit
reads the code with the assumptions that wrote it.

| ID | Pattern | Check before dispatch |
|----|---------|----------------------|
| C1 | `@tags: path-input; provable; scope:diff` **Single-gate path trust boundary** — validating a path-shaped field with one rule (e.g. `if value.contains("/")`) lets slashless inputs (`".."`, `"tmp"`, `"src"`) bypass the check and resolve against cwd. Caught as a CRITICAL slashless-bypass in the codesight MCP build. | Classify every path-shaped field into three tiers and validate accordingly: **(1) Identifier** (`repo` accepting `"engram"` OR an absolute path) — only path-form gets the prefix check. **(2) Filesystem path** (`path`, `repoPath`, `storagePath`) — ALWAYS `path.resolve` + require result under the trusted prefix, slash or not. **(3) Repo-relative** (`filePath`, `pathPrefix`) — reject any `..` segment via `path.posix.normalize` + segment split. Ask "identifier or filesystem path?" for each field; the rules differ. |
| C2 | `@tags: sentinel-semantics; provable; scope:diff` **New `validate()`/guard rejects a value that is a valid sentinel elsewhere** — adding a "reject 0/empty/-1" rule without checking how the field is consumed. E.g. rejecting `rebuild_interval == 0` when `sync.rs` guards `if rebuild_interval > 0 { rebuild }`, so `0` means "disabled" — the validation turns a supported config into a startup error. Caught on the axon code-scan remediation diff (CODE-MED-5). | Before adding a "reject N" rule, grep every consumer of the field (`> 0`, `== 0`, `is_empty()`, `.unwrap_or`, match arms). If ANY consumer treats the rejected value as a sentinel/disable/default mode, do not reject it. A scan finding that says "reject 0" may be wrong about the value being invalid — verify against usage. |
| C3 | `@tags: ci-config; provable; scope:diff` **CI step runs a venv-dependent tool without an active virtualenv** — `maturin develop`, `pip install -e .`, etc. fail on a clean runner because `actions/setup-python` provides an interpreter, NOT a venv. The job goes red on every push before tests run. Caught on the axon `python-bindings` CI job (TEST-HIGH-3). | Any new/edited CI step invoking `maturin develop` or other venv-requiring tools must create + activate a venv first (`python -m venv .venv && . .venv/bin/activate` in the SAME `run:` block, since each step is a fresh shell — or persist via `$GITHUB_PATH`/`$GITHUB_ENV`). You usually can't run the job locally, so this is a read-time check. |
| C4 | `@tags: lossy-stash; reasoning-shape; scope:both` **A "lossy transform made safe by a full-fidelity stash/backup" feature breaks its own retrievability guarantee in three coupled ways — stashing an already-LOSSY projection instead of the raw source, appending the retrieval POINTER to a body that is then HEAD-truncated (clipping the pointer when output is largest), and running the destructive transform even when the stash write SILENTLY FAILED.** When a feature compresses/redacts/summarizes data and claims "the full version is retrievable via the stash," all three invariants must hold or the claim is false exactly in the worst case. Caught on the Baxter MCP-output `compact_json` plan: (1) it stashed an already-lossy text projection (non-text content → `"[type content omitted]"`, `structuredContent` dropped) instead of `serde_json::to_string(&res)`, so many results were unrecoverable; (2) the `(full … saved to .baxter/outputs/…)` note was appended then passed through head-only `trunc_chars`, so a large body clipped the note off and killed the retrieval path; (3) the empty/unwritable-root path still did destructive compaction while claiming retrievability. | For any feature whose safety story is "lossy inline view + full stash for retrieval," verify three things before dispatch: (a) **stash the RAW source**, not a display/filtered projection — trace what the projection drops and confirm the stashed bytes contain everything it lost; (b) **the retrieval pointer must survive truncation** — never append a load-bearing pointer to a body that is then head/tail-truncated; reserve budget (`cap.saturating_sub(note.len())`) and append the note AFTER the cap (or prepend it), with a test that a max-size body still ends/starts with the pointer; (c) **gate the destructive transform on stash SUCCESS** — if the backup write fails or is unavailable, fall back to the prior NON-destructive behavior (plain truncation keeping a contiguous prefix), so "transformed ⇒ retrievable" is an invariant, not a hope. Also: when adding the treatment to a success path, check whether the ERROR path needs it too (large error diagnostics are outputs), and gate the error path on ITS OWN lossy condition (its display cap actually dropping content), not a threshold copied from the success path. |
| C5 | `@tags: test-hermeticity; provable; scope:diff` **A test that drives a LIVE external dependency (on-disk DB, real index, network, daemon) sits in the DEFAULT test suite behind only an "absent → skip" guard — so it is fast/green when the dependency is ABSENT but runs SLOW/nondeterministically (or hangs) when the dependency is PRESENT, making `cargo test`/CI non-hermetic.** The author reasons "it skips cleanly when absent," but the skip-guard only fires on the ABSENT branch; on the machine where the resource exists (the dev box, a warm CI cache), the test executes the real expensive path inside the default run. Caught on the Axon symbol-precision diff: an in-crate test called the real retriever → `AxonService::open` on the on-disk RocksDB index; it "skipped" only when the index was missing/locked, but on an indexed workspace `cargo test` sat >60s on it — and local runs that hit a held DB-lock skip path masked the hang. Fix: mark it `#[ignore = "requires a real index; run with --ignored"]` / env-gate / feature-gate so it is OPT-IN, never in the default suite — keep the absent-resource skip-guard as belt-and-suspenders for the opt-in run. | For any test that touches a live external resource (real DB/index/filesystem-state/network/daemon — not an in-memory or temp fixture), it must NOT run in the default `cargo test`/`pytest`/CI suite. An "if absent, skip" guard is insufficient: it only covers the absent branch, so the test is non-hermetic exactly where the resource exists. Gate it behind `#[ignore]` / a feature flag / an env var so the default suite is deterministic regardless of local state, and keep the skip-guard for the explicit opt-in invocation. Ask: "if this resource IS present and slow, does the default suite still finish fast and deterministically?" If not, it's not a default-suite test. (This is RUNTIME hermeticity of a test that compiles fine — beware the inverse-masking trap: a locked/absent local resource makes it skip, hiding the hang from the author.) |
| C10 | `@tags: command-grammar; provable; scope:both` **A security guard that ENUMERATES a command's option grammar to resolve a command-position (allow/deny) models only a SUBSET of each flag's spellings — short flag but not its long alias, or it omits a NON-EXEC mode — so a catastrophe behind the unmodeled spelling dilutes past the guard, and a non-exec mode mis-parses a benign trailing token into a false catastrophic argv0 (over-deny).** When a classifier hand-models a CLI's options to decide whether the rest is a wrapped command (exec-wrapper unwrap, sudo/privilege detection, an interpreter `-c` carve-out), modeling `-x` but not `--exclusive`, `-c` but not `--class`, `-w` but not `--wait` leaves the long form unhandled → the wrapped catastrophe falls to the ambiguous branch (here ASK, but in an allow-context could be silent). Symmetrically, treating a NON-EXEC selector (`chrt -p PID`, `ionice -p`, `chrt -m`/`--max` print-and-exit, `flock -c STRING`) as if it wrapped a command mis-binds a positional/trailing token as the inner argv0 → over-deny. Caught on the Baxter EXEC_WRAPPER unwrap: the first impl modeled short flags only; Codex found `flock --exclusive … rm -rf /`, `env --ignore-environment sudo reboot`, `setsid --wait`, `chrt --fifo`, `ionice --class`, `xargs -J` all diluting deny→ask, plus `chrt -m`/`ionice -p` over-denying. | For any guard that hand-models a command's option grammar, enumerate EACH modeled flag's COMPLETE spelling set — short `-x`, long `--alias`, `--long=value`, and combined `-xVALUE` — from the actual man page (don't model one spelling and assume the rest); a missed alias is a silent grammar hole. Separately, list the command's NON-EXEC / print-and-exit / operate-on-existing-PID / packed-string modes and route them to the SAFE-ambiguous state (ASK/None), never unwrap them as if a command follows. Verify the dominance property both ways: no catastrophe slips via an unmodeled common spelling (dilution), and no benign non-exec form newly denies (over-deny). Pairs with P33 (ambiguous→ask, not deny-on-guess) — C10 is the enumeration-completeness half: P33 says "don't guess," C10 says "model the whole real grammar so you don't have to." **Three spelling axes beyond short-vs-long that recur (Baxter 06-03, 3 Codex rounds): (1) CLUSTERED short-option bundles — a value/significant flag hidden mid-cluster (`bash -exc CMD`, `taskset -ac LIST CMD`, `watch -bt`) is NOT an exact-token match; decompose every single-dash cluster into its letters. (2) `--` END-OF-OPTIONS — model it explicitly: for a wrapper whose command follows (`watch -- CMD`, `taskset -- MASK CMD`) `--` means \"rest is the command\" → must still resolve/deny; for a flag-that-takes-a-string (`sh -- script`) it means \"rest is NOT options\". (3) OPTION-REGION TERMINATION — an option-scan hunting a flag (e.g. shell `-c`) must STOP at `--` and at the first non-option positional, else a later positional that looks like the flag (`bash script.sh -c CMD`, where `script.sh` is the script and `-c CMD` are its args) is mis-read → over-deny. Enumerate clustered + `--` + termination, not just short/long/=/combined.** |
| C11 | `@tags: select-threading; metric-aggregate; provable; scope:both` **A plan that adds an ALTERNATIVE score/weight/priority (`rerank_score`, an override, a boost) and threads it through EXISTING selection code via `new.unwrap_or(old)` has three silent failure modes the plan usually misses: (a) it must reach EVERY raw-old-value read site or the feature is PARTIALLY INERT; (b) refactoring an existing tie-break (e.g. first-wins "keep first chunk == best within EPSILON") into `max_by(new)` changes the chosen element ON TIES even when the new value is absent, breaking any "byte-identical when off" claim; (c) a non-finite `new` value (NaN/Inf from a model/division) POISONS the `>` and `partial_cmp(..).unwrap_or(Equal)` comparators it now flows into — `NaN > x` is false and `(NaN-best).abs()<EPS` is false, so a selector can keep NOTHING.** Caught on the Axon reranker spike design: file survival is decided by raw `chunk.score` in THREE places (per-file dedup best-chunk `budget.rs:194-215`, max-files eviction `:228-240`, budget greedy `:108-130`); the plan named only "dedup + budget" (two), and the "net-zero when off" story rested on a refactor that, written naively as `max_by`, would have changed dedup's representative chunk on score ties with the reranker disabled. NaN from the new f32 source would silently empty a file's chunk set. | For any plan adding an alternative/override value consumed by existing ranking/selection/eviction code: (1) grep EVERY read of the old field across the selector(s) and confirm the effective-value reaches ALL of them — list the sites by file:line, don't say "the selection logic" (a missed site = partially-inert feature, the inverse of a no-op); (2) centralize as ONE accessor (`effective_x()`) used by all comparators, never hand-threaded `unwrap_or` per closure; (3) preserve the EXISTING tie-break semantics verbatim — if the code is "first-wins within EPSILON," keep first-wins (swap only the value read), do NOT swap to `max_by`/`sort` which reorders ties; (4) guarantee the new value is FINITE at the write boundary (clamp/reject NaN/Inf) before it enters any `>`/`partial_cmp` site, at a SINGLE sanitization boundary — do NOT split it across producer+caller with contradictory contracts (a producer that remaps `NaN→NEG_INFINITY` is still emitting a non-finite value whose doc claims "finite only"; pick one site, usually the caller, and have it reject non-finite → fall back to the old value); (5) the "byte-identical when off" claim needs a dedicated pre-feature-vs-feature-disabled equality test — an upstream golden (here `fuse()`) or a sibling-parity test does NOT cover the post-feature selector refactor, AND a run-to-run determinism / self-comparison test is ALSO insufficient (it compares post-change code to itself and misses regressions the new wiring introduced) — you must capture a FROZEN pre-edit baseline snapshot BEFORE any selector edit lands and assert exact equality against that constant. Pairs with P30 (read the helper BODY) and P31 (None-sentinel semantics): C11 is the write side — a new optional value flowing INTO existing comparators. |
| C12 | `@tags: concurrency-lock; provable; scope:both` **A cross-process lock built on a WALL-CLOCK TTL guarding a SYNCHRONOUS (no-await) critical section is a nest of distinct concurrency bugs that surface one at a time — unconditional release stomps a reclaimer's lock, a `setInterval` "heartbeat" never fires (the event loop is blocked by the synchronous holder), a fixed TTL evicts a still-LIVE long-running owner, and the read→delete→insert reclaim is a cross-process TOCTOU.** Each is real and distinct; fixing one reveals the next. Caught on engram ADR-001 sync run-lock across many Codex rounds: (1) `release()` did `DELETE WHERE id=1` unconditionally → after a TTL-reclaim it deleted the new owner's row → needed an owner LEASE TOKEN, `release` scoped `WHERE token=?`; (2) the heartbeat was `setInterval(renew, …)` but `sync()` has zero `await` so the timer can't run → had to renew INLINE in the work loop; (3) inline renew still didn't cover non-loop phases, and ANY fixed TTL reclaims a live owner whose sync legitimately exceeds it; (4) the correct model is LIVENESS-based — store `owner_pid`, reclaim only when `!isProcessAlive(pid)` (`process.kill(pid,0)`/ESRCH), never on age; (5) even then the dead-row reclaim `DELETE WHERE id=1` races cross-process (two callers read the same dead pid, A reclaims, B's delete wipes A's fresh row) → reclaim must be COMPARE-AND-SWAP `DELETE WHERE id=1 AND owner_pid=? AND locked_at=?` on the OBSERVED values. | For any cross-process mutex in SQLite (or any check-then-act lock): (a) NEVER release/reclaim with an unconditional `DELETE WHERE <pk>` — scope every delete to the identity you hold/observed (lease token for release; observed `owner_pid`+`locked_at` for reclaim) so it's a compare-and-swap, not a blind wipe; (b) do NOT use `setInterval` to keep a lock alive across a SYNCHRONOUS critical section — the event loop is blocked, the timer is dead code (refresh inline at work checkpoints, or don't rely on refresh); (c) prefer LIVENESS (owner PID + `kill(pid,0)`) over a wall-clock TTL — a fixed TTL either evicts a live long-running owner (overlap) or strands a crash (pick your failure), whereas liveness never evicts a live owner and reclaims a dead one immediately; keep a generous TTL only as a documented PID-reuse backstop, and even then gate it so it can't override a confirmed-live owner; (d) make table creation `CREATE TABLE IF NOT EXISTS` so two first-runs on an upgraded DB don't race on the check-then-create. Note: in a single process better-sqlite3 is synchronous so same-process callers serialize — the races are all CROSS-process (multiple daemons/CLIs on one DB file), which is exactly when the lock matters. |
| C13 | `@tags: default-flip; reasoning-shape; scope:both` **A change that flips a DEFAULT MODE (or adds a behavior gate) gates the MAIN code path but leaves SIDE-EFFECT branches that also act under the old default ungated — so the headline invariant the flip advertises is silently violated by a less-traveled branch.** The author flips the obvious loop and writes a test for the common path; the rare side-branches (deletions, cleanup passes, dry-run preview counters, error paths) still run the old behavior. Caught on engram ADR-001 pull-by-default: the flip + the marquee "pull makes ZERO vault writes" (AC-2) test covered create/update, but `deleted-on-engram` still called `moveToTrash` (engram→vault) on pull, the `nosync`-cleanup pass still trashed files on every non-dry sync, AND the dry-run `deleted.engram++` still counted a deletion the real pull wouldn't do — three separate AC-2 leaks the same-model audit missed because the test only exercised the main path. | For any default-flip / new behavior-gate, grep EVERY site that performs the gated side effect (here: every vault-mutating call — `moveToTrash`/`writeFile`/`unlink`/`rename`), not just the primary loop, and confirm each is under the SAME gate (or is genuinely in the other direction / DB-only). Enumerate the rare branches explicitly: deletions, tombstones, cleanup/GC passes, conflict paths, AND the dry-run/preview counters (a preview that reports an action the real run now skips is itself a bug). Write the invariant test to drive each side-branch (delete-on-X, tag-gains-flag, dry-run), not only create/update — the headline invariant ("pull = zero writes", "read-only mode", "feature-flag off") is only as true as its least-traveled branch. |
| C14 | `@tags: command-grammar; provable; scope:both` **An ALLOW-LIST flag-hardening pass (drop an auto-approved command to ask when it carries a dangerous flag) fails along THREE independent completeness axes that a single-model audit under-covers: (1) COMMAND-set — the audit enumerates SOME allowlisted commands' dangerous flags but misses OTHER allowlisted commands that ALSO have exec/write/indirect-read flags; (2) FLAG-SPELLING — a value-taking SHORT flag guarded by exact-match misses the ATTACHED/BUNDLED form (`-oFILE`, `-rno out`); (3) INTERPRETER open-endedness — a command whose exec-flag surface is large and version-growing (node: -e/-p/-r/-i/--eval/--import/--loader/--experimental-loader/…) can NEVER be safely closed by a blocklist.** Caught over 6 Codex rounds on the Baxter 06-02 allowlist-dangerous-flag plan: round 1 the audit missed file/wc/du/pytest entirely (axis 1) and `has_flag` exact-match let `sort -oFILE`/`file -fL`/`pytest -pX` through (axis 2); rounds 2-5 kept finding new node exec flags one at a time until node was rewritten as a positive WHITELIST (allow only `node --check`/`-c` with no other option), then a positional edge (`node -- --check app.js` runs a script literally named `--check`) needed a stateful leading-option parse; go was under-audited and leaked `-o`/`-c`/`-overlay`/`env -w`/`-args` test-binary passthrough writes. Final: 19 concrete bypasses across 6 fix rounds, all same-model-audit-clean before Codex. | For any allow-list hardening (auto-approve → ask on a dangerous flag): **(axis 1)** enumerate EVERY allowlisted command, not just the obvious ones — for EACH, check its real man/help for exec, arbitrary-file-WRITE (`-o`/`--output`/compile/profile), and indirect-out-of-root-READ (`--files-from`/`--files0-from`/list-of-paths) flags; missing a whole command is the easiest gap. **(axis 2)** a value-taking SHORT flag must be matched by "single-dash cluster CONTAINS the letter" (catches `-oV`, bundled `-rno`), never exact `== "-o"`; a long flag needs both `--x` and `--x=` (cf. C10). **(axis 3)** if a command is an INTERPRETER or has an open-ended/version-growing exec-flag surface, do NOT blocklist its exec flags (whack-a-mole that loses on the next version) — use a positive WHITELIST: allow ONLY the one known-safe invocation shape, parsed STATEFULLY by argument position (options end at `--` or the first positional; a flag-looking token after that is a filename, not a flag). Direction must be fail-safe (allow→ask only) so every miss degrades to a human gate, never to silent allow. Pairs with C10 (flag-spelling completeness) and P33 (fail toward the safe state). |

| C15 | `@tags: migration-parity; reasoning-shape; scope:both` **A migration/parity test that recreates the PRE-migration schema as a fixture must reproduce that schema's CONSTRAINTS and ALL its objects (indexes, triggers, defaults) faithfully — an infidelity either makes the test setup IMPOSSIBLE (seed rows that violate the old constraint can't be inserted) or makes the parity gate BLIND (a comparison of only `{name,type,notnull}` misses `dflt_value` drift and skips non-table objects entirely).** Two coupled failure modes from the same root. Caught on engram P0 sync graph_id plan: (a) the legacy fixture built `UNIQUE(vault_path)` then the multi-graph test inserted TWO rows with the same `vault_path` — impossible under that very index (the duplicate must be inserted only AFTER the migration rebuilds it as composite `(graph_id, vault_path)`); the fixture also omitted the real `sync_log` indexes + prune trigger, so upgrade coverage was unfaithful. (b) the parity test compared only `{name,type,notnull}` and a single index — it would miss `dflt_value` divergence (the ALTER path MUST carry `DEFAULT 'x'` because SQLite requires a default for `ADD COLUMN NOT NULL`, while a fresh `CREATE TABLE` can omit it → defaults diverge, `notnull` does NOT catch it) and miss the other indexes + the trigger. A bogus "the notnull bit forces default alignment" note made it worse: SQLite reports `notnull=1` for `x TEXT NOT NULL` WITHOUT any default. | For any migration/parity test: (1) build the legacy fixture as a VERBATIM copy of the real pre-migration DDL — every column default, every index, every trigger, every constraint — not a simplified subset; seed data that the OLD constraints permit, and exercise the NEW constraint's relaxation only AFTER the migration runs (insert the now-legal duplicate post-migration). (2) A schema-parity comparison must cover `dflt_value` (not just name/type/notnull) AND every non-table object (indexes, triggers) via normalized `sqlite_master.sql`. (3) Never reason "`notnull` proves the default matches" — `ADD COLUMN NOT NULL DEFAULT x` and `CREATE ... NOT NULL` both report `notnull=1` but differ on `dflt_value`; the default is the drift axis a column-shape check misses. Pairs with C21-class dual-schema parity (multiple DDL sources must converge). |
| C16 | `@tags: tenant-isolation; reasoning-shape; scope:both` **Deferring tenant/graph-isolation on a table by arguing "FK `ON DELETE CASCADE` bounds its rows to the parent's tenant" conflates row LIFETIME with row ACCESS — the cascade only deletes child rows when the parent dies; it does NOTHING to scope SELECT/UPDATE/DELETE-by-global-id, which remain cross-tenant.** A table whose every read returns all tenants' rows, or whose mutation addresses a row by a global autoincrement id, is a cross-tenant read/write hole regardless of any FK. Caught on engram P0 sync graph_id plan: `sync_conflicts` was deferred ("FK-CASCADE bounds it transitively"), but `getConflicts()`/`getStatus()` read ALL graphs' conflicts and `resolveConflict(conflictId, ...)` could mark ANOTHER graph's conflict resolved by global id — a cross-tenant WRITE, the exact class the PR existed to close. The deferral rationale was factually wrong and would have shipped the hole. | When deciding whether a table needs a `graph_id`/tenant column, do NOT accept "an FK already bounds it" — FK CASCADE governs row deletion on parent-delete, not query scope. Ask the access questions instead: does any SELECT return rows across tenants? does any UPDATE/DELETE address a row by a GLOBAL id (autoincrement PK) without a tenant predicate? If yes to either, the table needs the scope column and every query needs the predicate — it is in scope, not deferrable. A table slated for future removal is still a live hole until removed. Pairs with C13 (default-flip leaves a side-branch ungated) — both are "the headline isolation/invariant is only as true as its least-scoped access path." |
| C17 | `@tags: path-equality; provable; scope:both` **A helper that decides whether two file paths refer to the SAME file via a bare `ends_with`/substring suffix test (no path-SEPARATOR boundary check) OVER-MATCHES segment-unaligned suffixes — `src/foobar.rs` "matches" `bar.rs`, `crates/axon-rank/src/budget.rs` "matches" `rank/src/budget.rs` — and the bug scales with the helper's cross-reference fan-out.** Bidirectional `a.ends_with(b) || b.ends_with(a)` over normalized paths correctly solves absolute-vs-repo-relative matching (a leading `/` is preserved, so the relative side is a suffix of the absolute side), which is why it passes the obvious positive tests — but it admits two false-match classes: (1) a shorter name that is a non-`/`-aligned tail of a longer one (`foobar.rs` ⊃ `bar.rs`), and (2) two different files sharing a trailing path segment-run with a non-aligned boundary. When the helper is used to attribute/classify by cross-referencing one path against EVERY symbol in a result set (not a single membership test), the false-match surface is large and silently mis-buckets — here it would inflate the exact metric the probe exists to measure (a confirmation-bias trap). "An existing sibling helper uses the same `ends_with`" is NOT license to copy it: the existing one may have a tiny fan-out (one expected-list membership test) while the new one fans out across the whole result set. Caught on the Axon anchor-provenance probe plan: `same_file()` cross-ref'd FP paths against all anchors + structural symbols; Codex flagged the suffix collisions the same-model plan review missed. | For any helper that tests path EQUALITY/identity by suffix, require SEGMENT ALIGNMENT: after normalizing, accept exact-eq OR a suffix match where the character before the matched suffix in the longer path is a `/` (e.g. `long.strip_suffix(short).is_some_and(|p| p.is_empty() || p.ends_with('/'))`), bidirectionally if either side may be absolute. Add NEGATIVE tests for unaligned collisions (`foobar.rs`↔`bar.rs`) and different-dir same-basename (`a/b.rs`↔`c/b.rs`), not just the positive absolute-vs-relative case. Audit the fan-out: a suffix matcher that cross-references against MANY candidates is far higher-risk than a single membership test — "a sibling helper already does it this way" does not transfer when the fan-out differs. Pairs with C1 (path trust boundary — there the risk is traversal/bypass; here it's silent over-match in attribution/classification). |
| C18 | `@tags: metric-aggregate; reasoning-shape; scope:both` **A metric DEFINED at one aggregation granularity (file/module/user) but IMPLEMENTED by matching/keying at a FINER granularity (exact symbol/line/row) silently mis-aggregates: the coarse rollup is STARVED whenever the fine unit that survived (post filter/dedup/budget) is not the one carrying the coarse unit's signal — and the error is DIRECTIONAL, usually toward the catch-all bucket, which is often the author's PRIOR (a confirmation-bias trap).** Same-model audits read the code with the assumption that wrote it ("match the symbol, roll up by file") and miss that the rollup is starved. Caught on the Axon anchor-provenance probe: `classify_provenance_refined` required `anchor.symbol_name == fp.symbol` before deciding a wrong FILE was anchor-reached, then `provenance_file_counts` rolled up per file by strongest provenance. But once budget/dedup runs, a wrong file's surviving FP chunk is often a DIFFERENT symbol than the anchor seed — so a file genuinely reached via an anchor on `helper_a`, surviving as `helper_b`, was classified `SemanticOnly`, mis-bucketing the file away from `WrongAnchor` toward `SemanticOnly` — inflating exactly the bucket the prior (D190) predicted dominant. Four same-model auditors (spec/simplify/harden/QA) missed it; Codex caught it. | For any metric whose REPORTED unit is coarse (file/module/package/user) but whose IMPLEMENTATION matches/keys/filters on a FINER unit (symbol/line/row/session): confirm the coarse rollup cannot be starved — would the coarse unit's signal be MISSED if the specific fine unit that survives filtering/dedup/budget is not the one carrying it? Match at the metric's OWN granularity (test FILE membership, not exact-symbol identity). Then identify the DIRECTION of any residual mis-aggregation and verify it does NOT bias toward the author's hypothesis / a catch-all bucket. Add a test where the surviving fine-unit differs from the signal-carrying fine-unit within the same coarse unit. Pairs with C17 (segment-aligned path matching for the file test) and P31 (don't infer a unit's class from its empty/partial output). |

## Tag schema & vocabulary

Each entry's Pattern cell is prefixed with an inline-code `@tags:` token (additive; no content
change; no renumber; table-safe because an inline-code span contains no `|`). Greppable via
``grep -o '@tags:[^`]*'``. Token grammar:

`@tags: <category>[; <category>…]; <provable|reasoning-shape>; scope:<plan|diff|both>[; floor]`

- **category** — one or more values from the closed enum below. An off-enum category means the
  engine read an unrecognized token — treat the entry as untagged (floor-default).
- **provable** — a grep can RELIABLY confirm this category ABSENT, so a confirmed-absent grep may
  dismiss the entry.
- **reasoning-shape** — a grep CANNOT confirm absence; the entry is NEVER dismissed, it always
  deep-checks. (`reasoning-shape` describes what proves NON-applicability, not what the check does —
  the entry's own "Check before dispatch" may still involve greps/reads.)
- **scope** — `plan` (prose reviews), `diff` (code reviews), or `both`.
- **floor** — reserved for always-applicable entries (P1–P4 plan-symbol concerns; any
  untagged/unclassifiable entry defaults here). A `floor` entry is always `applicable` WHEN IN SCOPE — scope-mismatch is checked first, so a `scope:plan` floor entry (P1–P4) is correctly scope-dismissed in a pure `diff` review.

**Category enum (17):**
`plan-symbol · negative-existence · ambiguity-guard · helper-reuse · metric-aggregate · path-input ·
path-equality · sentinel-semantics · ci-config · lossy-stash · test-hermeticity · command-grammar ·
select-threading · concurrency-lock · default-flip · migration-parity · tenant-isolation`

## Grep battery (provable categories only)

A signal FIRES for a category if ANY pattern below matches the diff hunks (DIFF mode) or the plan
prose (PLAN mode). Patterns are deliberately BROAD — over-matching only costs a cheap `✓`;
under-matching risks a silent skip, which is the dangerous direction. `reasoning-shape` categories
have NO battery (they always floor). Each pattern set is DISTILLED from the named category's entries'
existing "Check before dispatch" prose — do NOT invent new criteria.

| Category (provable) | Entries | Fire if ANY matches |
|---------------------|---------|---------------------|
| `path-input` | C1 | field name `(path\|dir\|file\|repo\|root\|prefix\|dest\|src)` OR a call `path.resolve\|open(\|fs::\|include_str!\|Path(\|join(` on a string OR a single-gate `.contains("/")` |
| `path-equality` | C17 | `ends_with\|starts_with\|strip_suffix\|strip_prefix\|contains(` used to compare TWO paths for sameness, OR a `same_file`/`is_same`/path-identity helper |
| `sentinel-semantics` | C2 | a new `validate`/guard rejecting `0`/`empty`/`-1`/`< 1`, OR `is_empty()\|== 0\|reject\|return Err` near a config/interval/threshold field |
| `ci-config` | C3 | touched `.github/workflows/*.yml\|Makefile\|noxfile\|tox.ini` OR `maturin\|pip install -e\|setup-python\|venv\|activate` |
| `test-hermeticity` | C5 | a test (`#[test]\|def test_\|it(\|fn test`) that opens a real DB/index/socket/daemon (`open(\|connect\|::open\|RocksDB\|sqlite\|TcpStream\|reqwest\|index`) without a temp/in-memory fixture |
| `command-grammar` | C10, C14 | a guard/classifier that models CLI flags (`has_flag\|starts_with("-")\|--\|arg ==\|match flag\|allowlist\|deny`) OR an exec-wrapper/interpreter unwrap (`exec\|sudo\|env \|node -e\|sh -c\|-c \|--eval`) |
| `select-threading` | C11 | a new alternative score/weight/override threaded via `unwrap_or\|max_by\|sort_by\|partial_cmp\|rerank\|boost` into existing selection |
| `metric-aggregate` | P31, C11 | an `Option`-returning per-unit metric whose aggregate skips `None` (`filter_map\|flatten\|\.iter().filter(.*is_some\|skip None\|average\|mean`) |
| `concurrency-lock` | C12 | `lock\|mutex\|acquire\|release\|TTL\|setInterval\|lease\|owner_pid\|kill\(pid\|INSERT OR IGNORE\|DELETE WHERE id` OR a `CREATE TABLE` whose name matches `(lock\|lease\|sync\|consolidation)` |

`metric-aggregate` also appears on P31; P31 is `provable` (battery above) but its sibling reasoning
about zero-vs-N/A is part of its deep-check, not its dismissal. `command-grammar` on P3 is `floor`
(a command named in plan PROSE — no diff to grep), whereas on C10/C14 it is `provable` (a real diff
exhibits the grammar).

## Audit-line format

The engine emits exactly this block before dispatch. Every live entry appears EXACTLY ONCE, in
`applicable` OR `dismissed` (G-account). `floor-note` is an ADDENDUM annotating which `applicable`
IDs got there by floor rather than a signal match — floor IDs are NOT listed twice and are NOT a
third bucket.

```
Pre-flight scope:
  mode: <plan|diff>
  signals: <category(evidence), …>
  applicable(N): <IDs> → ✓ <ids> | FIXED <ids>     (includes floor + reasoning-shape + matched)
  dismissed(M): <ID N/A(scope-mismatch:<entry-scope> vs mode:<plan|diff>)> | <ID N/A(no-signal:cat, evidence:<grep>)> …
  total: N + M = <live count>   floor-note: of the applicable, <ids> were floored (not signal-matched)
```

Each dismissed entry carries EXACTLY ONE of two reasons: `scope-mismatch:<entry-scope> vs mode:<plan|diff>`
(its `scope:` excludes the current mode) or `no-signal:<cat>, evidence:<grep that confirmed absence>`
(in-scope `provable` category proven absent). No other dismissal reason is valid — there is no third
bucket and no third reason. `<live count>` is COMPUTED from the file at run time — never a hardcoded
number. If `N + M` does not equal the live entry count, an entry was dropped: recount and re-emit.

## Banned rationalizations (pre-flight engine)

These are bypasses, not exemptions:
- "Bulk-dismiss this whole category, it obviously doesn't apply" — BANNED. Every entry is accounted
  for BY ID; you may dismiss an individual `provable` entry only with a confirmed-absent grep cited
  as evidence. Bulk-by-category dismissal reintroduces the skip loophole.
- "This change is simple/trivial/small, skip the floor" — NO. Size NEVER exempts a floor entry.
  P1–P4 always deep-check on plan reviews regardless of perceived simplicity.
- "I read this `reasoning-shape` entry and it's fine, so dismiss it" — NO. Reading-to-rule-out IS
  the deep-check; its home is `applicable`→`✓`, never `dismissed`. `reasoning-shape` entries are
  never dismissed.
- "This entry has no `@tags` yet, so skip it" — NO. Untagged ⇒ floor (keystone) ⇒ always
  deep-checked.
- "Empty applicable-set, so the change is clean, pass it" — NO. An empty applicable-set on a change
  with ≥1 non-comment code line is a FINDING → escalate to a full check (G-empty/G-escalate).

## Notes for learning capture

- Append a new row only when a finding is **recurring** (would apply to other plans/code), not a
  one-off project-specific logic error.
- If pre-flight missed something Codex then caught, the pattern description here is too weak —
  tighten the existing row rather than adding a near-duplicate.
- Keep "Check before dispatch" mechanical and verifiable — a step someone can actually run, not a
  principle to keep in mind.
- Every new entry is BORN TAGGED: in the same edit that adds the row, prepend its `@tags:` token
  with a category from the enum + a `provable`/`reasoning-shape` marker + `scope:`. If no existing
  category fits, ADD the new category to BOTH the enum in `## Tag schema & vocabulary` AND (if it is
  `provable`) a battery row in `## Grep battery` — in the SAME edit.
- Until an entry is tagged, it FLOORS (always deep-checked) by the keystone rule in SKILL.md — so a
  newly-appended, not-yet-tagged entry is never silently skipped. The growth race cannot produce a
  silent skip.
- NEVER reuse a live ID; continue the sequence with the next un-occupied P##/C##.
