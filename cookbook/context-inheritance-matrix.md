# Context Inheritance Matrix

This matrix closes the CS-5 retro action item (2026-03-25 decision: `memory/decisions/2026-03-25-context-inheritance-as-matrix.md`). Every code-touching agent must receive the style guide and design defaults — not just task-specific context. The matrix makes gaps visible: an empty cell where a code-touching agent does not receive a shared reference file is an obvious defect.

**Reading the cells:** Each cell states HOW the agent receives the file — the mechanism matters. "INSERT placeholder, orchestrator paste" means the orchestrator reads the file and fills a named section in the dispatched prompt. "Read in planning.md Context Inheritance" means the planning worker's own instructions tell it to Read the file directly. "Read directly — pointer line" means the agent's OWN instruction file names the path and tells the agent to Read it itself — no orchestrator paste step exists. "—" means the agent does not receive this file and that is intentional (e.g., read-only reviewers need task context, not shared reference files).

**Live agent roster** (as of this revision): `ct-implementer` (code-touching implementer, all dispatch paths including MICRO tasks — there is no separate builder agent) and five read-only reviewers: `ct-spec-reviewer`, `ct-qa-reviewer`, `ct-harden-auditor`, `ct-simplify-auditor`, `ct-spec-doc-reviewer`. Earlier revisions of this matrix named a now-retired MICRO-task builder agent and two generic review-role placeholders — none of these three names exist in the current roster (the retired builder's responsibilities were absorbed by `ct-implementer`; the two generic placeholders now map to the five distinct named reviewers above). The table below uses the live names only.

| Reference file | planning-worker | design-team (Team Leader + workers) | ct-implementer | read-only reviewers (ct-spec-reviewer, ct-qa-reviewer, ct-harden-auditor, ct-simplify-auditor, ct-spec-doc-reviewer) |
|---|---|---|---|---|
| `~/.claude/golden-principles.md` | Read in `planning.md` Context Inheritance item 1 | Read in `design-team-context-retrieval.md`, passed as "## Golden Principles" section | INSERT placeholder via `execution.md` step 2 (architectural decisions tasks) | — (read-only reviewers) |
| `~/.claude/code-style.md` | Read in `planning.md` Context Inheritance item 2 | Read in `design-team-context-retrieval.md`, passed as "## Code Style" section | INSERT placeholder via `execution.md` step 2 (code-language tasks) | — (read-only reviewers) |
| `docs/team-memory.md` | Read in `planning.md` Context Inheritance item 3 (if exists) | Read in `design-team-context-retrieval.md`, passed as "## Team Memory" section | Relevant entries via `execution.md` step 2 Context section | — (read-only reviewers) |
| `~/.claude/skills/coding-team/skills/second-opinion/codex-learnings-digest.md` (design face — author-facing "what to do") | — (not wired; `planning.md` Context Inheritance has no codex item — see note below) | — (not wired; see note below) | Read directly — pointer line in `ct-implementer.md`'s own "## Codex Design Defaults" section ("Read `.../codex-learnings-digest.md` before implementing..."). This is an instruction-pointer the agent follows itself, NOT an INSERT placeholder and NOT an orchestrator-paste step — `execution.md` does not reference codex anywhere. | — (design face is author-facing; reviewers receive the review face instead — see next row) |
| `~/.claude/skills/coding-team/skills/second-opinion/codex-learnings-review-digest.md` (review face — reviewer-facing "what to check") | — | — | — | Read directly — pointer line in `ct-spec-reviewer.md`, `ct-qa-reviewer.md`, and `ct-harden-auditor.md` (each: "Before reviewing, Read `.../codex-learnings-review-digest.md` — the check-face of recurring Codex-caught defects; cite any that apply"). Instruction-pointer, not an INSERT/orchestrator-paste. **Gap** — `ct-simplify-auditor.md` and `ct-spec-doc-reviewer.md` have NO such pointer line; see note below. |

## Gaps Identified

The following cells are empty (or partially empty) where wiring was arguably expected. These are noted for follow-up.

**ct-simplify-auditor.md / ct-spec-doc-reviewer.md — codex-learnings-review-digest.md:** three of the five live reviewers (`ct-spec-reviewer`, `ct-qa-reviewer`, `ct-harden-auditor`) have a direct "Read the review digest before reviewing" pointer line; `ct-simplify-auditor` and `ct-spec-doc-reviewer` do not. Follow-up: add the same pointer line to both.

**planning-worker / design-team — codex-learnings-digest.md (design face):** neither `planning.md`'s Context Inheritance list nor `design-team-context-retrieval.md` reference the design digest. The design digest is generative — it helps authors avoid mistakes while writing plans/code. The design-team produces design docs (not implementation plans or code) and the planning-worker produces task lists (not implementation code), so the need is lower but not zero: both write implementation recommendations/tasks that feed directly into what `ct-implementer` builds and may encode the same recurring mistakes. This gap is a judgment call deferred to a future iteration. Follow-up: evaluate whether the planning-worker and design-team should receive the design digest as an advisory section.

## Verification Evidence

Each cell was verified by reading the actual source files, not inferred:

- `planning.md` Context Inheritance: verified at lines 93–103 (items 1–4; there is no item 5 — a prior revision of this matrix claimed a "item 5" codex reference that does not exist in the live file)
- `design-team-context-retrieval.md`: verified lines 16, 50, 56 (golden principles, code style sections present; team-memory handling likewise present)
- `execution.md` step 2: verified at lines 82–84 (code-style, golden-principles, team-memory paste instructions present); verified via full-file grep that `execution.md` contains ZERO references to "codex" — the design-digest paste step this matrix previously claimed does not exist
- `ct-implementer.md` Code Style INSERT: verified at lines 86–90
- `ct-implementer.md` "## Codex Design Defaults" section: verified at lines 92–94 — a direct `Read` instruction-pointer written into the agent's own file, not an INSERT placeholder
- `ct-spec-reviewer.md`, `ct-qa-reviewer.md`, `ct-harden-auditor.md`: each verified via grep to contain a direct `Read .../codex-learnings-review-digest.md` pointer line before their review instructions
- `ct-simplify-auditor.md`, `ct-spec-doc-reviewer.md`: verified via grep to contain ZERO references to "codex-learnings" — the review-digest gap above
- Live agent roster: verified via directory listing of `agents/` (`ct-harden-auditor.md`, `ct-harness-engineer.md`, `ct-implementer.md`, `ct-plan-doc-reviewer.md`, `ct-prompt-craft-auditor.md`, `ct-qa-reviewer.md`, `ct-simplify-auditor.md`, `ct-spec-doc-reviewer.md`, `ct-spec-reviewer.md`); no retired-builder or generic-reviewer-placeholder agent file exists in this listing, and `commands/build.md` (previously cited for a MICRO-task builder dispatch path at "lines 60-64") is now a 24-line thin alias to `/coding-team` with no builder-specific logic at all — confirming the retired builder role left no trace, not merely a rename.
