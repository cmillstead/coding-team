# Hook Bypass Rule

Hooks are authoritative. If one blocks, comply. If one errors, stop and report the error. Any "different approach" that avoids a hook's constraint is a bypass — including exploiting a parsing gap. If the hook is wrong, the user fixes the hook.
